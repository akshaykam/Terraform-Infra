apiVersion: v1
kind: Service
metadata:
  name: productapp-ui
spec:
  selector:
    app: productapp-ui
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  type: {{ .Values.service.type }}